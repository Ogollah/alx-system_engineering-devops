sudo ufw default deny incoming   # Set the default incoming policy to deny all
sudo ufw default allow outgoing  # Set the default outgoing policy to allow all
sudo ufw allow 22/tcp           # Allow SSH (port 22)
sudo ufw allow 80/tcp           # Allow HTTP (port 80)
sudo ufw allow 443/tcp          # Allow HTTPS (port 443)
sudo ufw enable
